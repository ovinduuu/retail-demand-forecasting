"""Model training and evaluation code.

Roadmap Phase 3: baseline (naive / seasonal-naive) models, feature engineering
over the dbt marts, a LightGBM training script with CLI args, evaluation
(WRMSSE / MAPE), and experiment tracking via Vertex AI Experiments.

Planned modules: baseline.py, features.py, train.py, evaluate.py
"""
